<UserControl x:Class="IshchenkoKMAPractice4.Views.EditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Label Content="Email: " HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10,10,0,0"/>
        <TextBox HorizontalAlignment="Left" Height="23" TextWrapping="Wrap" VerticalAlignment="Top" Width="210" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Margin="80,10,0,0" RenderTransformOrigin="0.517,0.565"/>
        <Button Content="{Binding ButtonName}" HorizontalAlignment="Left" VerticalAlignment="Top" Width="80" Command="{Binding ProceedCommand}" Margin="35, 65, 0,0" Height="20"/>
        <Button Content="Cancel" HorizontalAlignment="Left" VerticalAlignment="Top" Width="80" Command="{Binding CancelCommand}" Margin="175,65,0,0"/>
    </Grid>
</UserControl>